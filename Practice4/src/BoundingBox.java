
public class BoundingBox {
	/*
	 * BoundingBox is a lightweight class designed
	 * to hold the boundaries of a Shape object 
	 * in a stanard way.  
	 * x and y represent the top left coordinate of a
	 * box circumscribing the shape.
	 * height and width are the dimensions of that bounding
	 * box.
	 */
public int x;
public int y;
public int height;
public int width;
}
